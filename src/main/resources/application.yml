spring:
  application:
    name: face-recognition
  profiles:
#    active: dev
    active: devuto
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    open-in-view: true
